Contour (외곽선)
경계면 상에서 연속되는 점들을 이어그린 선
단순히 이미지의 테두리 부분을 모두 뽑아내는 edge와는 달리 각 object들을 구분하는 선이다
이때문에 모양분석과 객체 탐색에 주로 사용된다

Contour detection
Contour detection은 opencv에서 제공하는 FindContours 함수를 통해 진행된다.

Hierarchy 계급, 체계
Retrieval 검색, 회수